#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int main() {
    ios::sync_with_stdio(false);
    ll a, b;
    cin >> a >> b;
    cout << a * b - a - b << endl;    
    return 0;
}